<div class="card-list-container py-5">
  <div class="row">
    <% items.each do |item| %>
      <div class="col-md-3 mt-4">
        <div class="card-body">
          <div class="row">
            <div class="col-md-12 text-left">
              <% if item.image.attached? %>
                <%= link_to item_path(item) do %>
                  <%= image_tag item.image, width: 200, height: 400 %>
                <% end %>
              <% else %>
                <%= link_to item_path(item) do %>
                  <%= image_tag "no_image.jpg", width: 200, height: 400 %>
                <% end %>
              <% end %>
            </div>
            <div class="col-md-12 text-left">
              <p><%= item.name %></p>
            </div>
            <div class="col-md-12 text-left">
              <div class="price">
                ￥<%= number_with_delimiter(item.price, precision: 0) %>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <div class="row justify-content-center">
    <%= paginate items %>
  </div>
</div>